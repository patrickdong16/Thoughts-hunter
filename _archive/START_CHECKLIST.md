# ✅ 思想雷达 CMS - 启动检查清单

## 🎯 现在要做的事（按顺序）

### ☐ 步骤1：确认PostgreSQL已安装并运行

选择以下任一方式：

**选项A：Homebrew**
```bash
brew install postgresql@15
brew services start postgresql@15
createdb thoughts_radar
```

**选项B：Docker**
```bash
docker run --name thoughts-radar-db \
  -e POSTGRES_PASSWORD=mypassword \
  -e POSTGRES_DB=thoughts_radar \
  -p 5432:5432 -d postgres:15

# 然后修改 backend/.env 的 DB_PASSWORD
```

**验证**：
```bash
psql thoughts_radar -c "SELECT 1;"
```

---

### ☐ 步骤2：初始化数据库

```bash
cd backend
npm run init-db
```

**期待输出**：
```
✅ Tables created successfully
✅ Data seeded successfully
📊 Database Statistics:
   - Bands: 16
   - Radar Items: 6
```

---

### ☐ 步骤3：启动后端API

**新终端窗口1**：
```bash
cd backend
npm run dev
```

**期待输出**：
```
╔═══════════════════════════════════════╗
║      思想雷达 API Server              ║
╚═══════════════════════════════════════╝

Server running on: http://localhost:3000
```

**验证**：
```bash
curl http://localhost:3000/health
```

应该返回：`{"success":true,"message":"Thoughts Radar API is running"...}`

---

### ☐ 步骤4：启动CMS

**新终端窗口2**：
```bash
cd cms
npm run dev
```

**期待输出**：
```
VITE v7.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
```

---

### ☐ 步骤5：访问CMS并测试

1. **打开浏览器**：http://localhost:5173

2. **应该看到**：
   - 顶部："思想雷达 CMS"标题（绿色渐变）
   - 两个按钮："📝 内容管理" 和 "📊 频段管理"
   - 下方显示今天日期的6条示例内容

3. **测试添加内容**：
   - 点击"✨ 添加内容"按钮
   - 填写表单（至少500字正文）
   - 点击"创建"
   - 应该看到新内容出现在列表中

4. **测试编辑TTI**：
   - 切换到"📊 频段管理"
   - 找到"T1"频段
   - 点击"编辑"
   - 修改TTI值（如改为90）
   - 点击"保存"

---

## ✅ 完成！

如果以上5个步骤都成功，恭喜！你的思想雷达CMS已经完全可用了！

---

## 🎯 接下来可以做什么

### 立即可做

1. **添加明天的内容**
   - 创建6-8条2026-01-20的内容
   - 覆盖不同的频段

2. **调整TTI值**
   - 根据当前时事调整各频段的张力指数

3. **探索API**
   - 访问：http://localhost:3000/tools/api-tester.html
   - 测试所有API端点

### 进阶开发

4. **开发用户端APP**
   - 使用现有API
   - 展示每日雷达内容
   - 实现收藏和表态功能

5. **增强CMS功能**
   - 添加内容搜索
   - 添加批量导入
   - 添加数据导出

6. **部署上线**
   - 后端部署到Railway/Render
   - CMS部署到Vercel/Netlify
   - 使用托管数据库（Supabase/Neon）

---

## 📚 快速参考

### 常用命令

```bash
# 后端
cd backend
npm run dev        # 启动开发服务器
npm run stats      # 查看数据统计
npm run check      # 环境检查

# CMS
cd cms
npm run dev        # 启动开发服务器
npm run build      # 构建生产版本
```

### 访问地址

- **CMS**: http://localhost:5173
- **API**: http://localhost:3000
- **API测试**: http://localhost:3000/tools/api-tester.html

### 文档位置

- **总览**: `/README.md`
- **后端**: `/backend/README.md`
- **CMS**: `/cms/README.md`
- **快速启动**: `/cms/QUICKSTART.md`

---

## 🐛 遇到问题？

### CMS显示"加载失败"

**原因**：后端没有运行

**解决**：
```bash
# 检查后端
curl http://localhost:3000/health

# 如果失败，启动后端
cd backend && npm run dev
```

### "数据库连接失败"

**原因**：PostgreSQL没有运行或配置错误

**解决**：
```bash
# 检查服务
brew services list | grep postgresql

# 检查配置
cat backend/.env

# 测试连接
psql thoughts_radar -c "\l"
```

### "至少需要500字"

**解决**：正文内容少于500字，继续添加直到达到要求

---

## 💡 提示

- ✅ 使用右下角绿色 `+` 按钮快速添加内容
- ✅ 编辑时会自动填充张力问题等信息
- ✅ 每日6-8条内容会显示绿色 ✓ 标识
- ✅ 同一天同一频段只能有一条内容

---

**准备好了吗？从步骤1开始吧！** 🚀
