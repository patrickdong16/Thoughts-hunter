# 服务器配置
NODE_ENV=development
PORT=3000

# 数据库配置
# 本地开发（单独配置）
DB_HOST=localhost
DB_PORT=5432
DB_NAME=thoughts_radar
DB_USER=postgres
DB_PASSWORD=your_password_here

# 生产环境（使用DATABASE_URL）
# DATABASE_URL=postgresql://user:password@host:port/database
# 示例：postgresql://postgres:password@localhost:5432/thoughts_radar

# CORS配置
# 本地开发
CORS_ORIGIN=http://localhost:5173,http://localhost:8081,http://localhost:19006

# 生产环境（部署后修改）
# CORS_ORIGIN=https://your-cms.vercel.app,https://your-app.com

# JWT密钥（如果需要用户认证）
# JWT_SECRET=your-secret-key-here-change-in-production
# JWT_EXPIRES_IN=7d

# API配置
# API_BASE_URL=http://localhost:3000

# 其他配置
# RATE_LIMIT_MAX=100
# RATE_LIMIT_WINDOW_MS=900000
