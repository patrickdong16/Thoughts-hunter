{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixPkgs": [
      "python312",
      "ffmpeg-headless"
    ],
    "buildCommand": "pip install yt-dlp && npm ci"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}