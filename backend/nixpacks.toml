# Railway Nixpacks 配置
# 安装 Python 和 yt-dlp 用于获取 YouTube 字幕

[phases.setup]
nixPkgs = ["python312", "ffmpeg-headless"]

[phases.install]
cmds = [
    "python3 -m pip install --user yt-dlp",
    "npm ci"
]

[start]
cmd = "npm start"
